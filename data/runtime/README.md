# Runtime State

The local SQLite database, worker state, logs, and browser QA profiles are generated here and
ignored by Git. Production deployments require an approved persistent database and backup policy.
